# 🚀 빠른 시작 가이드

## 📋 프로젝트 현황

### ✅ 완료된 작업
- [x] 프로젝트 디렉터리 구조 생성
- [x] 5개 Java 클래스 스켈레톤 코드 작성
- [x] 샘플 입력 파일 (input1.txt, input2.txt)
- [x] 문서 (README, 협업 가이드, 아키텍처)
- [x] .gitignore 파일

### ⏳ 구현 필요 사항
- [ ] Person 1: Graph.java TODO 구현
- [ ] Person 1: GraphTraversal.java TODO 구현
- [ ] Person 2: GraphFileReader.java TODO 구현
- [ ] Person 2: Dijkstra.java TODO 구현

---

## 🎯 지금 바로 시작하기

### Step 1: 프로젝트 확인 (30초)
```bash
cd /Users/david/Workspace/DisceteMath_Prj
ls -R src/
```

**예상 출력:**
```
src/:
graph

src/graph:
Dijkstra.java  Graph.java  GraphFileReader.java  GraphTraversal.java  Main.java
```

---

### Step 2: 역할 확인 (1분)

#### 👤 Person 1 (그래프 + 탐색)
**작업 파일:**
1. `src/graph/Graph.java` - 4개 TODO
2. `src/graph/GraphTraversal.java` - 2개 TODO

**작업 시간:** 약 2-3시간

#### 👤 Person 2 (파일 I/O + 최단 경로)
**작업 파일:**
1. `src/graph/GraphFileReader.java` - 2개 TODO
2. `src/graph/Dijkstra.java` - 2개 TODO

**작업 시간:** 약 2-3시간

---

### Step 3: 구현 시작 (2-3시간)

#### Person 1 작업 순서

**3-1. Graph.java 열기**
```bash
# 편집기로 파일 열기
vi src/graph/Graph.java
# 또는
code src/graph/Graph.java
```

**3-2. TODO 찾기 및 구현**
- `addEdge(int from, int to)` 구현
- `addEdge(int from, int to, int weight)` 구현  
- `hasEdge(int from, int to)` 구현

**힌트:** `COLLABORATION_GUIDE.md`에 구현 예시 있음!

**3-3. GraphTraversal.java 구현**
- `dfsRecursive()` 구현
- `bfs()` 구현

---

#### Person 2 작업 순서

**3-1. GraphFileReader.java 열기**
```bash
vi src/graph/GraphFileReader.java
# 또는
code src/graph/GraphFileReader.java
```

**3-2. TODO 찾기 및 구현**
- `readUnweightedGraphs()` 구현
- `readWeightedGraphs()` 구현

**힌트:** `COLLABORATION_GUIDE.md`에 파싱 로직 예시 있음!

**3-3. Dijkstra.java 구현**
- `dijkstra()` 알고리즘 구현
- `Result.getPath()` 경로 복원 구현

---

### Step 4: 컴파일 (1분)

```bash
cd /Users/david/Workspace/DisceteMath_Prj
javac src/graph/*.java
```

**성공 시:** 아무 메시지 없음  
**실패 시:** 에러 메시지 확인 후 수정

---

### Step 5: 실행 (1분)

```bash
java -cp src graph.Main
```

**예상 출력:**
```
1. 그래프 탐방 수행 결과

그래프 [1]
----------------------------
깊이 우선 탐색
1 – 2 – 4 – 8 – 5 – 6 – 3 – 7 
너비 우선 탐색
1 – 2 – 3 – 4 – 5 – 6 – 7 – 8 
============================

그래프 [2]
----------------------------
깊이 우선 탐색
...
```

---

## 📚 문서 둘러보기 (5분)

### 필수 문서 (반드시 읽기)
1. **README.md** (5분)
   - 전체 프로젝트 개요
   - 실행 방법
   - 입출력 형식

2. **COLLABORATION_GUIDE.md** (10분)
   - **구체적인 구현 코드 예시**
   - 2인 협업 방법
   - 체크리스트

### 참고 문서 (선택)
3. **ARCHITECTURE.md** (5분)
   - 클래스 다이어그램
   - 실행 흐름
   - 복잡도 분석

4. **PROJECT_SUMMARY.md** (3분)
   - 프로젝트 요약
   - 체크리스트
   - FAQ

---

## 💡 구현 팁

### 🔑 핵심 포인트

#### 1. 인접 행렬은 1-indexed
```java
// ✅ 올바른 방법
Graph graph = new Graph(5, false);
// adjMatrix는 [6][6] 크기 (0번 인덱스 미사용)

// ❌ 틀린 방법
// adjMatrix를 [5][5]로 생성하면 ArrayIndexOutOfBoundsException
```

#### 2. 방문 순서는 오름차순
```java
// ✅ 올바른 방법
for (int i = 1; i <= n; i++) {
    if (!visited[i] && graph.hasEdge(current, i)) {
        // 작은 번호부터 방문
    }
}

// ❌ 틀린 방법
for (int i = n; i >= 1; i--) {
    // 큰 번호부터 방문하면 결과가 다름
}
```

#### 3. Dijkstra 갱신 조건
```java
// ✅ 올바른 방법
if (newDist < distance[v]) {
    distance[v] = newDist;
    parent[v] = u;
}

// ❌ 틀린 방법
if (newDist <= distance[v]) {
    // 같을 때도 갱신하면 먼저 찾은 경로가 유지 안 됨
}
```

#### 4. 출력 형식
```java
// ✅ 올바른 방법
sb.append(" – ");  // 공백-하이픈-공백

// ❌ 틀린 방법
sb.append("-");    // 하이픈만
sb.append(" - ");  // 일반 하이픈 (요구사항은 en dash)
```

---

## 🐛 디버깅 가이드

### 컴파일 에러

**증상:** `package graph does not exist`
```bash
# 해결: 올바른 위치에서 컴파일
cd /Users/david/Workspace/DisceteMath_Prj
javac src/graph/*.java
```

**증상:** `cannot find symbol`
```bash
# 해결: TODO 구현이 완료되지 않음
# 해당 메서드를 구현하세요
```

---

### 실행 에러

**증상:** `FileNotFoundException: input1.txt`
```bash
# 해결: 실행 위치 확인
cd /Users/david/Workspace/DisceteMath_Prj
ls input1.txt  # 파일 존재 확인
java -cp src graph.Main
```

**증상:** `NullPointerException`
```bash
# 해결: TODO 구현 누락
# 모든 TODO를 구현했는지 확인
grep -r "TODO" src/
```

---

### 출력 불일치

**증상:** DFS/BFS 순서가 다름
```
# 원인: 인접 정점을 오름차순으로 방문하지 않음
# 해결: for문을 1부터 n까지 순회
```

**증상:** 최단 경로가 다름
```
# 원인: Dijkstra 갱신 조건이 <= 사용
# 해결: < 만 사용 (같을 때 갱신 금지)
```

---

## 📞 도움 받기

### 막힐 때 참고할 순서

1. **TODO 주석 읽기**
   - 각 메서드에 구현 힌트가 있습니다

2. **COLLABORATION_GUIDE.md 확인**
   - 구체적인 구현 코드 예시가 있습니다

3. **요구사항_명세서.md 재확인**
   - 상세한 요구사항을 다시 읽어보세요

4. **파트너와 상의**
   - 서로 도와가며 해결하세요

---

## ✅ 완료 확인

### 구현 완료 체크

```bash
# 1. 컴파일 성공
javac src/graph/*.java
echo "컴파일 성공!"

# 2. 실행 성공
java -cp src graph.Main

# 3. 출력 확인
# - "1. 그래프 탐방 수행 결과" 나오는지
# - "2. 최단 경로 구하기 수행 결과" 나오는지
# - 정점 구분자가 " – " 인지
```

### 최종 제출 전

- [ ] 모든 TODO 구현 완료
- [ ] 컴파일 에러 없음
- [ ] 실행 시 예외 발생 안 함
- [ ] DFS/BFS 출력 형식 일치
- [ ] Dijkstra 출력 형식 일치
- [ ] 여러 그래프 처리 정상
- [ ] 주석 및 코드 정리

---

## 🎉 성공 기준

### 최소 요구사항
- ✅ 프로그램이 실행됨
- ✅ input1.txt, input2.txt를 읽음
- ✅ DFS/BFS 결과 출력
- ✅ Dijkstra 결과 출력
- ✅ 출력 형식이 요구사항과 일치

### 추가 점수
- ✅ 코드 가독성 (의미 있는 변수명)
- ✅ 주석 (핵심 로직 설명)
- ✅ 에러 처리 (파일 없을 때 등)
- ✅ 효율적인 구현

---

## 🚀 다음 단계

### 기본 구현 완료 후
1. 다양한 테스트 케이스 작성
2. 예외 상황 처리 강화
3. 코드 리팩토링
4. 성능 최적화 (선택)

### 심화 학습
- 우선순위 큐를 이용한 Dijkstra (O(ElogV))
- A* 알고리즘
- Floyd-Warshall (모든 정점 쌍 최단 경로)
- Bellman-Ford (음수 가중치 처리)

---

**행운을 빕니다! 화이팅! 💪**

---

## 📖 Quick Reference

### 컴파일 & 실행
```bash
cd /Users/david/Workspace/DisceteMath_Prj
javac src/graph/*.java
java -cp src graph.Main
```

### 파일 구조
```
Person 1: Graph.java, GraphTraversal.java
Person 2: GraphFileReader.java, Dijkstra.java
공통: Main.java (이미 완성)
```

### 문서 우선순위
1. README.md - 필수
2. COLLABORATION_GUIDE.md - 필수 (구현 예시)
3. QUICKSTART.md - 이 파일
4. ARCHITECTURE.md - 선택
5. PROJECT_SUMMARY.md - 선택

