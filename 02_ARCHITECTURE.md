# 프로젝트 아키텍처

## 📐 클래스 다이어그램

```
┌─────────────────────────────────────────────────────────────────┐
│                           Main.java                              │
│                     (실행 진입점, 흐름 제어)                        │
└────────────┬─────────────────────────────┬──────────────────────┘
             │                             │
             │                             │
   ┌─────────▼──────────┐        ┌────────▼─────────────┐
   │ GraphFileReader    │        │  Graph               │
   │  (Person 2)        │        │  (Person 1)          │
   │                    │        │                      │
   │ + readUnweighted() │◄───────┤  - numVertices       │
   │ + readWeighted()   │        │  - adjMatrix[][]     │
   └─────────┬──────────┘        │  - isWeighted        │
             │                   │                      │
             │ creates           │  + addEdge()         │
             │                   │  + hasEdge()         │
             │                   │  + getWeight()       │
             │                   └──────┬───────┬───────┘
             │                          │       │
             └──────────────────────────┘       │
                                                │ uses
                      ┌─────────────────────────┼────────────────┐
                      │                         │                │
            ┌─────────▼─────────┐    ┌──────────▼────────┐      │
            │ GraphTraversal     │    │  Dijkstra         │      │
            │  (Person 1)        │    │  (Person 2)       │      │
            │                    │    │                   │      │
            │  + dfs()           │    │  + dijkstra()     │      │
            │  + bfs()           │    │  + Result         │      │
            │  + formatPath()    │    │    - distance[]   │      │
            └────────────────────┘    │    - parent[]     │      │
                                      │  + getPath()      │      │
                                      │  + formatPath()   │      │
                                      └───────────────────┘      │
                                                                 │
                                                                 │
                                      (Graph 객체 사용)            │
                                                  └──────────────┘
```

---

## 🔄 실행 흐름

### 전체 실행 시퀀스

```
                    [프로그램 시작]
                           │
                           ▼
                 ┌─────────────────┐
                 │   Main.main()   │
                 └────────┬────────┘
                          │
        ┌─────────────────┴─────────────────┐
        │                                   │
        ▼                                   ▼
  ┌──────────┐                       ┌──────────┐
  │  PART 1  │                       │  PART 2  │
  │ DFS/BFS  │                       │ Dijkstra │
  └─────┬────┘                       └─────┬────┘
        │                                  │
        │                                  │
        ▼                                  ▼
```

### Part 1: 그래프 탐방 (DFS/BFS)

```
1. GraphFileReader.readUnweightedGraphs("input1.txt")
   │
   ├─► 파일 열기
   ├─► 그래프 개수(N) 읽기
   ├─► Graph 객체 생성 (무가중치)
   ├─► N개 줄 파싱 → addEdge() 호출
   └─► List<Graph> 반환
   
2. 각 그래프에 대해:
   │
   ├─► GraphTraversal.dfs(graph, 1)
   │   ├─► visited[] 배열 초기화
   │   ├─► dfsRecursive() 재귀 호출
   │   └─► 방문 순서 List 반환
   │
   ├─► GraphTraversal.bfs(graph, 1)
   │   ├─► Queue 초기화
   │   ├─► 큐 기반 탐색
   │   └─► 방문 순서 List 반환
   │
   └─► 출력 (formatPath 사용)
```

### Part 2: 최단 경로 (Dijkstra)

```
1. GraphFileReader.readWeightedGraphs("input2.txt")
   │
   ├─► 파일 열기
   ├─► 그래프 개수(N) 읽기
   ├─► Graph 객체 생성 (가중치)
   ├─► 인접 정보 파싱 → addEdge(from, to, weight)
   └─► List<Graph> 반환
   
2. 각 그래프에 대해:
   │
   ├─► Dijkstra.dijkstra(graph, 1)
   │   ├─► distance[], parent[] 초기화
   │   ├─► 최소 거리 정점 선택
   │   ├─► 인접 정점 거리 갱신
   │   └─► Result 객체 반환
   │
   ├─► 각 정점 v에 대해:
   │   ├─► result.getPath(v)
   │   │   ├─► parent 역추적
   │   │   └─► 경로 List 반환
   │   │
   │   └─► 출력 (formatPath 사용)
   │
   └─► 다음 그래프
```

---

## 🧩 모듈 의존성

### Person 1 모듈
```
Graph.java
    ↓ (provides)
GraphTraversal.java
    ↓ (uses Graph)
Main.java (Part 1)
```

**특징:**
- Graph가 완성되어야 GraphTraversal 테스트 가능
- 독립적으로 개발 후 통합 가능

### Person 2 모듈
```
Graph.java
    ↓ (provides)
GraphFileReader.java
    ↓ (creates Graph)
Dijkstra.java
    ↓ (uses Graph)
Main.java (Part 2)
```

**특징:**
- Graph의 interface만 알면 개발 가능
- FileReader와 Dijkstra는 독립적

### 통합 포인트
```
Person 1                    Person 2
   │                           │
   ├─► Graph.java ◄────────────┤
   │                           │
   ├─► GraphTraversal  ────┐   │
   │                       │   │
   │                   Main.java
   │                       │   │
   └───────────────────────┘   │
                               │
                    ┌──────────┤
                    │          │
              FileReader    Dijkstra
```

---

## 💾 데이터 흐름

### 무가중치 그래프 (input1.txt → DFS/BFS)

```
input1.txt
    ↓ (파일)
GraphFileReader.readUnweightedGraphs()
    ↓ (파싱)
Graph 객체 (adjMatrix[i][j] = 0 or 1)
    ↓ (전달)
GraphTraversal.dfs() / bfs()
    ↓ (탐색)
List<Integer> (방문 순서)
    ↓ (포맷)
출력: "1 – 2 – 3 – 4"
```

### 가중치 그래프 (input2.txt → Dijkstra)

```
input2.txt
    ↓ (파일)
GraphFileReader.readWeightedGraphs()
    ↓ (파싱)
Graph 객체 (adjMatrix[i][j] = weight or INF)
    ↓ (전달)
Dijkstra.dijkstra()
    ↓ (계산)
Result (distance[], parent[])
    ↓ (복원)
List<Integer> (경로)
    ↓ (포맷)
출력: "정점 [2]: 1 – 3 – 2, 길이: 7"
```

---

## 🎯 핵심 알고리즘

### 1. DFS (깊이 우선 탐색)
**시간 복잡도:** O(V²) (인접 행렬)
**공간 복잡도:** O(V) (재귀 스택)

```
DFS(v):
  1. v를 방문 처리
  2. v를 결과에 추가
  3. v의 인접 정점들을 오름차순으로:
     - 미방문이면 DFS 재귀 호출
```

### 2. BFS (너비 우선 탐색)
**시간 복잡도:** O(V²) (인접 행렬)
**공간 복잡도:** O(V) (큐)

```
BFS(start):
  1. start를 큐에 추가
  2. 큐가 빌 때까지:
     a. 정점 v를 큐에서 꺼냄
     b. v를 결과에 추가
     c. v의 인접 정점을 오름차순으로 큐에 추가
```

### 3. Dijkstra (최단 경로)
**시간 복잡도:** O(V²) (우선순위 큐 미사용)
**공간 복잡도:** O(V) (distance, parent 배열)

```
Dijkstra(start):
  1. distance[start] = 0
  2. V번 반복:
     a. 미방문 중 최소 거리 정점 u 선택
     b. u 방문 처리
     c. u의 인접 정점 v에 대해:
        - 새거리 = distance[u] + weight[u][v]
        - 새거리 < distance[v]면 갱신
```

---

## 📊 복잡도 분석

### 공간 복잡도

| 구성 요소 | 크기 | 설명 |
|-----------|------|------|
| 인접 행렬 | O(V²) | `int[V+1][V+1]` |
| visited 배열 | O(V) | `boolean[V+1]` |
| distance 배열 | O(V) | `int[V+1]` |
| parent 배열 | O(V) | `int[V+1]` |
| 결과 리스트 | O(V) | `List<Integer>` |

**총 공간 복잡도:** O(V²)

### 시간 복잡도 (V = 정점 수, E = 간선 수)

| 알고리즘 | 인접 리스트 | 인접 행렬 | 본 구현 |
|---------|------------|----------|---------|
| DFS | O(V + E) | O(V²) | **O(V²)** |
| BFS | O(V + E) | O(V²) | **O(V²)** |
| Dijkstra | O((V+E)logV) | O(V²) | **O(V²)** |

---

## 🔒 주요 설계 결정

### 1. 인접 행렬 선택 이유
- ✅ 구현이 단순함
- ✅ 간선 존재 확인: O(1)
- ✅ 밀집 그래프에 적합 (V ≤ 100)
- ❌ 공간 복잡도: O(V²)

### 2. 1-indexed 배열 사용
- ✅ 정점 번호가 1부터 시작
- ✅ 직관적인 인덱싱
- ❌ 배열 크기 +1 필요

### 3. 파일 전체 읽기
- ✅ EOF까지 모든 그래프 처리
- ✅ 여러 테스트 케이스 일괄 실행
- ❌ 메모리 사용량 증가 (V ≤ 100이므로 문제 없음)

---

## 🛡️ 에러 처리 전략

### 파일 관련 에러
```java
try {
    // 파일 읽기
} catch (FileNotFoundException e) {
    System.err.println("파일을 찾을 수 없습니다");
} catch (IOException e) {
    System.err.println("파일 읽기 오류");
}
```

### 파싱 에러
```java
try {
    int n = Integer.parseInt(line);
} catch (NumberFormatException e) {
    System.err.println("잘못된 형식");
    continue;  // 다음 그래프 처리
}
```

---

## 🧪 테스트 전략

### 단위 테스트
1. **Graph 테스트**
   - 간선 추가 확인
   - 간선 존재 확인
   - 가중치 반환 확인

2. **GraphTraversal 테스트**
   - DFS 방문 순서
   - BFS 방문 순서
   - 포맷팅 확인

3. **Dijkstra 테스트**
   - 최단 거리 계산
   - 경로 복원
   - 포맷팅 확인

### 통합 테스트
1. input1.txt로 전체 실행
2. input2.txt로 전체 실행
3. 출력 형식 검증

---

**이 아키텍처는 2인 협업에 최적화되어 있으며, 복잡도를 최소화했습니다! 🎉**

